import { Container, SectionHeading, StatusBadge } from "../components/SiteShell";
const done = ["Docker 安装与基础网络修复", "Docker Compose 服务编排", "PostgreSQL / Redis / Caddy 部署", "Portainer 与 Hermes Agent 运行", "Cloudflare DNS 与 HTTPS", "Hermes 域名反向代理"];
const next = ["配置 Hermes 模型提供商", "接入 OpenAI / OpenRouter / Gemini", "部署微信小程序海报 API", "建立 Webhook 服务", "接入 PostgreSQL 与 Redis", "GitHub Actions 自动部署", "恢复浏览器自动化能力", "增加监控、备份与安全策略"];
export const metadata = { title: "路线图", description: "Flower ZC 的基础设施与项目推进路线图。" };
export default function RoadmapPage() { return <div className="py-36 md:py-44"><Container><SectionHeading eyebrow="Roadmap" title="已完成的，\n和下一步的。" description="路线图不是承诺交付时间，而是当前真实的工作顺序。状态会随着日志和部署结果更新。" /><div className="grid gap-12 lg:grid-cols-2"><div><div className="mb-5 flex items-center gap-3"><StatusBadge status="stable" /><h2 className="text-lg text-white/85">已完成</h2></div><div className="space-y-3">{done.map((item, index) => <div key={item} className="glass flex items-center gap-4 rounded-2xl p-4"><span className="font-mono text-xs text-emerald-200/65">{String(index + 1).padStart(2, "0")}</span><span className="text-sm text-white/75">{item}</span><span className="ml-auto text-emerald-200/70">✓</span></div>)}</div></div><div><div className="mb-5 flex items-center gap-3"><StatusBadge status="planned" /><h2 className="text-lg text-white/85">下一阶段</h2></div><div className="space-y-3">{next.map((item, index) => <div key={item} className="glass flex items-center gap-4 rounded-2xl p-4"><span className="font-mono text-xs text-[var(--accent)]">{String(index + 1).padStart(2, "0")}</span><span className="text-sm text-white/75">{item}</span><span className="ml-auto text-white/25">○</span></div>)}</div></div></div></Container></div>; }
