const nodes = ["Internet", "Cloudflare DNS", "Caddy / HTTPS", "Hermes · API · Blog · Portainer"];
export function ArchitectureFlow() { return <div className="glass overflow-hidden rounded-[1.5rem] p-5 md:p-8"><div className="mb-6 flex items-center justify-between"><span className="eyebrow">公开架构视图</span><span className="text-xs text-dim">不包含管理密钥</span></div><div className="grid gap-3 md:grid-cols-4 md:items-center md:gap-0">{nodes.map((node, index) => <div key={node} className="flex items-center gap-3 md:block md:text-center"><div className={`mx-auto flex h-16 w-full max-w-[190px] items-center justify-center rounded-2xl border ${index === nodes.length - 1 ? "border-[var(--accent)]/40 bg-[var(--accent)]/10" : "border-white/12 bg-white/[.04]"} px-3 text-sm text-white/80`}>{node}</div>{index < nodes.length - 1 && <span className="text-xl text-[var(--accent)] md:mt-3 md:block">→</span>}</div>)}</div></div>; }
